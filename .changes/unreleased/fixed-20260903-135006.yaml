kind: Fixed
body: |-
  Statement.Pipeline now returns the error when a task's status cannot be updated in its PodGroup (task not found in job) instead of logging it and continuing. Previously the allocate action treated such an attempt as successful and requeued the job forever, pinning the scheduler leader at 100% CPU until it was restarted.
