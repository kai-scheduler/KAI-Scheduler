# Copyright 2025 NVIDIA CORPORATION
# SPDX-License-Identifier: Apache-2.0

suite: queuecontroller podDisruptionBudget in kai-config

set:
  kaiConfigDeployer:
    enabled: false
  kaiConfig:
    render: true

templates:
  - kai-config.yaml
tests:
  - it: renders queuecontroller podDisruptionBudget from values
    set:
      queuecontroller.podDisruptionBudget:
        enabled: true
        maxUnavailable: 1
    asserts:
      - equal:
          path: spec.queueController.service.podDisruptionBudget.enabled
          value: true
      - equal:
          path: spec.queueController.service.podDisruptionBudget.maxUnavailable
          value: 1

  - it: renders disabled PDB when enabled is false
    set:
      queuecontroller.podDisruptionBudget:
        enabled: false
    asserts:
      - equal:
          path: spec.queueController.service.podDisruptionBudget.enabled
          value: false

  - it: omits podDisruptionBudget when the values map is absent
    set:
      queuecontroller.podDisruptionBudget: null
    asserts:
      - isNull:
          path: spec.queueController.service.podDisruptionBudget

  - it: renders explicit zero maxUnavailable
    set:
      queuecontroller.podDisruptionBudget:
        enabled: true
        maxUnavailable: 0
    asserts:
      - equal:
          path: spec.queueController.service.podDisruptionBudget.enabled
          value: true
      - equal:
          path: spec.queueController.service.podDisruptionBudget.maxUnavailable
          value: 0

  - it: renders custom maxUnavailable
    set:
      queuecontroller.podDisruptionBudget:
        enabled: true
        maxUnavailable: 2
    asserts:
      - equal:
          path: spec.queueController.service.podDisruptionBudget.enabled
          value: true
      - equal:
          path: spec.queueController.service.podDisruptionBudget.maxUnavailable
          value: 2
