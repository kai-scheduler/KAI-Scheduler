# Copyright 2025 NVIDIA CORPORATION
# SPDX-License-Identifier: Apache-2.0
{{- /*
This template is intentionally empty in production. The kai-config CR is
applied by the post-install/post-upgrade hook Job (see hooks/post/kai-config-deployer/)
using kubectl apply --server-side, so it is NOT a Helm release resource.
The chart unit tests under tests/ render the kai-config body to assert
chart values flow correctly into the operator input config; they enable
the standalone render via .Values._test.renderKaiConfig=true.
*/ -}}
{{- $testCfg := .Values._test | default dict -}}
{{- if $testCfg.renderKaiConfig -}}
{{ include "kai-scheduler.kai-config" . }}
{{- end -}}
